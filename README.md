# Run


```
bluetoothctl
scan on
devices
pair <dev hex address>
connect <dev hex address>
trust <dev hex address>
```

```
sudo apt install lsinput
ls /dev/input
# new device in output of ^ goes into detect.py
```

```
pip3 install requirements.txt
python3 detect.py
```
